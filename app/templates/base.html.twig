<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta
            name="viewport"
            content="width=device-width, initial-scale=1.0, user-scalable=no, minimum-scale=1.0, maximum-scale=1.0"
    />

    <title>tsotsCAT</title>
    <script src="https://cdn.jsdelivr.net/npm/admin-lte@3.2/dist/js/adminlte.min.js"></script>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/admin-lte@3.2/dist/css/adminlte.min.css">
</head>
<body class="hold-transition layout-top-nav">
    <div class="wrapper">
        <nav class="main-header navbar navbar-expand-md navbar-light navbar-white">
            <ul class="navbar-nav">
                <li class="nav-item d-none d-sm-inline-block">
                    <a href="{{ path('home') }}" class="nav-link">Home</a>
                </li>
                {% if is_granted('IS_AUTHENTICATED') %}
                    <li class="nav-item d-none d-sm-inline-block">
                        <a href="{{ path('auth_logout') }}" class="nav-link">Logout</a>
                    </li>
                    <li class="nav-item d-none d-sm-inline-block">
                        <a href="{{ path('project_index') }}" class="nav-link">Projects</a>
                    </li>
                {% else %}
                    <li class="nav-item d-none d-sm-inline-block">
                        <a href="{{ path('auth_login') }}" class="nav-link">Login</a>
                    </li>
                    <li class="nav-item d-none d-sm-inline-block">
                        <a href="{{ path('auth_register') }}" class="nav-link">Register</a>
                    </li>
                {% endif %}

            </ul>
        </nav>
        <div class="content-wrapper">
            {% block content %}
            {% endblock %}
        </div>
    </div>
</body>
